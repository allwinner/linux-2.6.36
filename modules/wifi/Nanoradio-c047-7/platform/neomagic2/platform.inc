KERNEL_DIR = $(NEOMAGIC2_KERNEL_DIR)
CROSS_COMPILE = $(NEOMAGIC2_CROSS_COMPILE)
KERNEL_MAJOR_VERSION=2.6
DEFAULT_BASEBAND_REVISION?=mobile

HOST_PLATFORM_SRC_DIR=mm6
TRANSPORT=sdio
HOST=arm-linux

ifeq ($(DEFAULT_BASEBAND_REVISION),mobile)
PLATFORM_FLAGS+=-DUSE_4BIT_SDIO
endif
