Freescale imx31 Nanoradio wifi driver version 0.rc58
====================================================

DELIVERABLES :

- nano_if.ko, nano_sdio.ko, zImage (kernel 2.6.19 - gcc-4.1.1-glibc-2.4-nptl-6
  compiler), x_mac.bin, nuttcp.freescale utility.


NOTES :
- Kernel 2.6.19 imx31 arch (preemption disabled, mmc support disabled).
- Plug n' Play support for the nanoradio card not supported.
- Driver uses first slot of the Freescale Platform (mmc1).
- 4-bit sdio suported.
- DMA feature not enabled.

How To :

In order to load the driver you do the following :

insmod nano_if.ko
insmod nano_sdio.ko
cat x_mac.bin >/proc/driver/eth1/firmware
iwconfig eth1 txpower -19


TODO :

- Enable DMA feature for the driver.

